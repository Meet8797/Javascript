const name = "Meet"
const repoCount = 50

// console.log(name + (repoCount) +" value");


console.log(`Hello my name is ${name} and repo count is ${repoCount}`);

const gameName = new String('Meet')

console.log(gameName.toLowerCase());
console.log(gameName.indexOf('t'));

